# ShulBoard — פיתוח

תיעוד טכני למי שרוצה לתרום קוד או להרים עותק משלו. התיאור למשתמש ב-[README](README.md).

## ארכיטקטורה

```
public/                 נכסים סטטיים
  index.html            דף הבית + הרשמה
  admin.html            ממשק הגבאים: אשף הקמה, לוח בית, ניווט צדי, גלריית מראות
  manage.html           דשבורד מנהל המערכת
  display.html          תבנית הצג (מוגשת דרך /s/<slug>)
  js/presets.js         קטלוג העיצוב — מראות, ערכות, גופנים, תבניות מסך, ערים
  js/api.js             לקוח הפלטפורמה
  js/{display,admin,site,manage}.js
  css/board.css         הצג: פריסה, ערכות צבע, סגנונות, גופנים, מקדם גודל טקסט
  css/traditional.css   הסגנון המסורתי המפואר
  css/screens.css       מסכים מתחלפים + חלון מודעות (נטען גם בצג וגם בניהול)
  css/admin.css         מעטפת הניהול, האשף, הגלריה, תצוגה מקדימה צמודה, מובייל
  css/{site,manage}.css

functions/              קוד שרץ בקצה
  _shared.js            סשנים, סיסמאות, זיהוי דייר, ברירות מחדל
  _migrations.js        גרסת סכימה, מיגרציות, ניקוי מקטעים, גרסת קוד לצג
  _templates.js         תבניות מהקהילה: יצירת טבלה עצלה, צילום המראה השמור
  api/templates/        רשימה ציבורית, פרסום, ספירת שימוש, הסרה
  _screens.js           מסכים בלייב: טבלה עצלה, רישום דופק, רשימה וספירה
  api/heartbeat/[slug].js  דופק מהצג (ציבורי, לא נשמר במטמון)
  api/screens.js        המסכים של בית הכנסת המחובר (מאומת)
  _middleware.js        כותרות אבטחה
  api/register.js       הרשמה + בדיקת זמינות כתובת
  api/{login,logout,me}.js
  api/data/             קריאה/כתיבה של מקטעי הגדרות (מאומת)
  api/public/[slug].js  המנה שהצג צורך (ציבורי, נשמר ב-cache 60 שניות)
  api/media/            העלאה, רשימה, מחיקה
  api/admin/            דשבורד מנהל: כניסה, סקירה, השהיה/מחיקה
  _admin.js             סשן מנהל — נפרד לגמרי מסשן הגבאים
  m/[id].js             הגשת קובץ מדיה (cache שנה)
  s/[slug].js           הגשת הצג של בית כנסת
  download/[file].js    מתקין מותאם לפי שם הקובץ

installer/shulboard.nsi  מקור המתקין ל-Windows
schema.sql               סכימת D1
```

### זיהוי בית הכנסת

`resolveSlug()` ב-`functions/_shared.js` בודק **קודם תת-דומיין ואז נתיב**.
היום עובד הנתיב `/s/<slug>`. ביום שיחובר דומיין משלכם, `beit-yaakov.example.com`
יעבוד מיד — בלי שינוי קוד.

### אימות

- **גבאים**: סיסמה משותפת לבית הכנסת, PBKDF2-SHA256 עם 100k סבבים ומלח לכל בית כנסת.
  סשן = טוקן אקראי ב-KV (12 שעות), עוגיית `sb_session` עם HttpOnly/Secure/SameSite.
  גבאי חדש נרשם אוטומטית בכניסה הראשונה.
- **מנהל המערכת**: ראו [דשבורד מנהל המערכת](#דשבורד-מנהל-המערכת) למטה.

## פניות מדף הבית

טופס "יצירת קשר" בדף הבית שולח ל-`POST /api/contact` (ציבורי). הפנייה נשמרת בטבלה
`contact` ומוצגת בדשבורד מנהל המערכת. אין תלות בשירות חיצוני, ואין כתובת מייל שמתפרסמת
באתר.

- **מייל חובה** (`isEmail` ב-`_contact.js`, אותה בדיקה בדיוק בדפדפן ובשרת); הטלפון רשות.
  בלי מייל אי אפשר לחזור לפונה בלי להסתמך על שיחה, וזה גם מה שמאפשר לזהות גבאי ששכח
  את סיסמת בית הכנסת. העמודה `email` נוספת לטבלה קיימת ב-`ALTER TABLE ... ADD COLUMN`
  בתוך `ensureContactTable`, עטוף ב-`catch` — SQLite זורק שגיאה אם היא כבר קיימת, וזה
  המצב הרגיל. פניות ישנות נשארות עם `email` ריק.
- הגנות: הגבלת קצב של 5 פניות לכתובת IP בשעה (KV), שדה מלכודת נסתר לבוטים
  (מקבל 200 בלי לשמור), חיתוך אורכים, ותקרה של 5,000 שורות.
- **התראה מיידית (רשות)**: הסוד `CONTACT_WEBHOOK` ב-Pages. אם הוגדר, כל פנייה נשלחת
  אליו גם כ-JSON (עם שדה `text` מוכן), כך שאפשר לחבר בוט טלגרם, Slack או Make/Zapier:

  ```bash
  wrangler pages secret put CONTACT_WEBHOOK --project-name shul-board
  ```

| נתיב | מי | מה |
|---|---|---|
| `POST /api/contact` | ציבורי | שליחת פנייה |
| `GET /api/admin/contact` | מנהל המערכת | 200 הפניות האחרונות, החדשות קודם |
| `PATCH /api/admin/contact/:id` | מנהל המערכת | `{status}` — `new` / `read` / `done` |
| `DELETE /api/admin/contact/:id` | מנהל המערכת | מחיקה |

## דשבורד מנהל המערכת

`/manage.html` — אזור מפעיל הפלטפורמה, נפרד לחלוטין מממשק הגבאים. מה יש שם: מוני
בתי כנסת/גבאים/מדיה, מדי ניצול מול המכסה החינמית, מסכים בלייב לכל בית כנסת, טבלת
בתי כנסת עם השהיה ומחיקה, גרף הרשמות ל-30 יום, ניהול תבניות מהקהילה, פניות מדף הבית,
ויומן פעולות מלא.

אבטחה: הסיסמה יושבת כסוד `PLATFORM_PASSWORD` ב-Pages (לא במסד), השוואה בזמן קבוע,
עוגייה נפרדת `sb_admin` עם סשן של 4 שעות, והגבלת קצב של 5 ניסיונות ל-IP ברבע שעה.
סשן של גבאי לא מקנה שום גישה לשם. בלי הסוד, הדשבורד מושבת (503).

מחיקת בית כנסת דורשת הקלדת ה-slug כאישור, מוחקת גם את הבייטים מ-KV, ומשאירה את
רשומות היומן בכוונה — עקבות מחיקה שווים יותר מניקיון.

### סיסמה שנשכחה

הסיסמה נשמרת כגיבוב PBKDF2 ולכן אי אפשר לשחזר אותה — אפשר רק לקבוע חדשה.

| נתיב | מי | מה |
|---|---|---|
| `POST /api/reset` | ציבורי | בקשת שחזור: שולח קישור למייל הרשום |
| `GET /api/reset-confirm?t=` | ציבורי | בדיקת תקינות הקישור לפני שמציגים טופס |
| `POST /api/reset-confirm` | ציבורי | `{token, password}` — קביעת הסיסמה |
| `PUT /api/shul` | גבאי מחובר | עריכת פרטי השחזור (`email`, `contact`) |
| `PUT /api/password` | גבאי מחובר | שינוי עצמי. דורש `{current, next}` — גם למי שכבר מחובר, כדי שעוגייה שנשארה פתוחה על המחשב בבית הכנסת לא תאפשר לעובר אורח לנעול את הגבאים בחוץ |
| `PUT /api/admin/shul/:id` | מנהל המערכת | איפוס ידני. דורש `{confirm: "<slug>"}` תואם, כמו במחיקה. מחזיר סיסמה חדשה **פעם אחת**, או מקבל `{password}` משלכם |

**שחזור עצמי במייל** (`_recovery.js`, `api/reset.js`, `api/reset-confirm.js`, `public/reset.html`):

- האסימון הוא 32 בתים אקראיים. ב-KV נשמר רק ה-**SHA-256** שלו (`reset:<hash>`), עם TTL
  של שעה. כך גם מי שקורא את ה-KV לא יכול לאפס סיסמה של אף בית כנסת — אותו היגיון
  שבגללו הסיסמאות עצמן הן גיבוב. אחרי קביעת סיסמה האסימון נמחק: קישור אחד, שימוש אחד.
- `POST /api/reset` מחזיר **תשובה זהה תמיד** — גם ל-slug שלא קיים וגם לבית כנסת בלי
  מייל רשום, וגם כשספק המייל נפל. אחרת הטופס היה הופך לכלי לגילוי אילו בתי כנסת קיימים.
- שתי מכסות: 5 בקשות ל-IP בשעה (מונע סריקה) ו-3 בקשות לאותו slug (מונע הצפת תיבה
  גם כשמחליפים IP).
- העמודה `shuls.email` נוספת ב-`ensureShulEmail()` (`ALTER TABLE` עטוף ב-catch), נקראת
  רק מהמסלולים שנוגעים בה. `shuls.contact` הפך לטלפון בלבד.

**כניסה עם Google** (`_google.js`, `api/auth/google/*`, `api/config.js`) — הדרך המומלצת:
חשבון משויך נותן זהות מאומתת בלי סיסמה לזכור, ומייתר את שחזור הסיסמה לגמרי.

- זרימת **Authorization Code**: הדפדפן חוזר עם `code`, והשרת מחליף אותו ל-`id_token`
  מול Google ישירות (TLS + `client_secret`). כיוון שהאסימון מגיע בערוץ הישיר הזה ולא
  דרך הדפדפן, אין צורך לאמת את חתימת ה-JWT — כך גם מתועד אצל Google. `aud` כן נבדק,
  כהגנה זולה מפני טעות הגדרה.
- `state` נשמר ב-KV ל-10 דקות ו**נמחק ברגע שנקרא**: אותו state לא יכול לשמש פעמיים.
- **שיוך** דורש סשן קיים — אחרת כל אחד היה משייך את עצמו לכל בית כנסת. טבלת
  `google_links` (יצירה עצלה) מחזיקה `sub` (מזהה Google, יציב גם אם המייל משתנה),
  מייל, ושם הגבאי שאיתו ייכנס בעל החשבון. `UNIQUE (shul_id, sub)` עם `ON CONFLICT
  DO UPDATE`, כך ששיוך חוזר מעדכן ולא משכפל.
- **מייל לא מאומת** (`email_verified !== true`) נדחה: הוא לא מזהה אף אחד.
- גבאי המשויך ל**כמה בתי כנסת** לא מנוחש: ה-callback מייצר אסימון בחירה קצר
  (חמש דקות, חד-פעמי) שמכיל בדיוק את בתי הכנסת שלו, והבחירה נעשית ב-`admin.html`.
  `POST /api/auth/google/pick` מוודא שה-`shulId` נמצא ברשימה שבאסימון.
- הסרת שיוך מותנית גם ב-`shul_id`, כך שגבאי לא יכול להסיר שיוך של בית כנסת אחר
  גם אם ניחש מזהה.
- **הרשמה עם Google** (`mode=register`): הזהות מאומתת לפני שידוע שם בית הכנסת, ולכן
  היא נשמרת לאסימון קצר (`gsignup:`, 20 דקות, חד-פעמי) והדפדפן חוזר לטופס ההרשמה
  עם `?gsignup=`. הטופס מצטמצם לשם ולכתובת — בלי סיסמה בכלל.
  - בית כנסת כזה נשמר עם **סיסמה אקראית שאיש אינו יודע** ועם `has_password = 0`
    (`ensureShulPasswordFlag`, ברירת מחדל 1 לכל מי שנרשם קודם). השיוך ל-Google נוצר
    באותה בקשה — אחרת היה נוצר בית כנסת בלי שום דרך להיכנס אליו.
  - `PUT /api/password` מוותר על "הסיסמה הנוכחית" כש-`has_password = 0`: אין כזאת
    לדעת, ובלי הוויתור הזה הבעלים היה תקוע בלי יכולת להגדיר סיסמה לעולם. אחרי
    ההגדרה הדגל עולה ל-1 והדרישה חוזרת.
  - `POST /api/login` דוחה כניסה בסיסמה ל-`has_password = 0` עם הודעה שמכוונת
    לכפתור Google. בפועל זו שורה מתה (צריך לנחש גיבוב אקראי כדי להגיע אליה),
    והיא קיימת כדי שאם המצב כן ייווצר, ההודעה תכוון במקום לבלבל.
- `GET /api/config` (ציבורי) מחזיר אילו תכונות אופציונליות דלוקות, כדי שהדפדפן ידע
  אם להציג את כפתורי Google עוד לפני שיש סשן.

```bash
# Google Cloud → Credentials → OAuth client (Web)
# Authorized redirect URI: https://<הדומיין>/api/auth/google/callback
wrangler pages secret put GOOGLE_CLIENT_ID     --project-name shul-board
wrangler pages secret put GOOGLE_CLIENT_SECRET --project-name shul-board
```

**ספק המייל** (`_mail.js`): ל-Workers אין שליחת מייל מובנית ואין SMTP — פורט 25 חסום,
וגם לקוח SMTP מעל `cloudflare:sockets` היה נתקל בחסימות של Google מול כתובות IP של
דאטה-סנטר. לכן שולחים דרך HTTP. שלושה ספקים, לפי הסודות שהוגדרו:

| | מתי | מגבלה |
|---|---|---|
| **Apps Script** (`GAS_MAIL_URL` + `GAS_MAIL_SECRET`) | כשאין דומיין — מומלץ | 100 נמענים ליום |
| **Brevo** (`BREVO_API_KEY` + `MAIL_FROM`) | חלופה בלי דומיין | 300 ליום, וחלק ינחת בספאם |
| **Resend** (`RESEND_API_KEY` + `MAIL_FROM`) | כשיש דומיין מאומת | — |

**למה Apps Script ראשון**: ספק חיצוני ששולח "מטעם" כתובת `@gmail.com` חותם SPF/DKIM
על הדומיין שלו, אין התאמת DMARC, וחלק מהמיילים נוחתים בספאם. הממסר
(`tools/gmail-relay.gs`) רץ **בתוך** חשבון Google, ולכן המייל יוצא באמת מהתיבה —
התאמה מלאה, בלי דומיין ובלי ספק חיצוני. הוראות ההתקנה בראש הקובץ.

שתי דקויות בצד שלנו (`sendViaAppsScript`):

- Web App של Apps Script עונה **302** אל `script.googleusercontent.com`, ושם הגוף
  האמיתי. `fetch` עוקב לבד, אז אין מה לעשות — רק לא להיבהל.
- הסקריפט מחזיר **200 גם כששליחה נכשלה אצלו**, ולכן הסימן להצלחה הוא גוף התשובה
  (`ok`) ולא קוד הסטטוס. בלי הבדיקה הזאת כישלון היה נראה כהצלחה.

בלי אף אחד מהסודות `POST /api/reset` מחזיר 503 עם הפניה לטופס יצירת הקשר, ושום
מסלול אחר לא מושפע.

הסיסמה שנוצרת באיפוס היא 10 תווים מאלפבית בלי `0/O` ובלי `1/l/i`, עם מקף באמצע —
היא נמסרת בהכתבה בטלפון. הדגימה עם דחייה (`limit = ⌊256/31⌋×31`) כדי שלא תהיה הטיה
לטובת התווים הראשונים. ביומן נרשמת רק העובדה שהייתה איפוס (`admin-reset-password`)
או שינוי (`password-change`) — לעולם לא הסיסמה עצמה.

הזיהוי של מי שמבקש איפוס נשען על `shuls.contact` — הטלפון/מייל שנרשם בהרשמה. הוא מוצג
למנהל בדיאלוג האישור, כדי להשוות אותו מול הפונה לפני שמאפסים. הדשבורד מציג את הסיסמה
החדשה בפאנל ייעודי (ולא ב-`alert`) כדי שאפשר יהיה להקריא ולהעתיק בנחת; סגירת הפאנל
מוחקת אותה, ואין דרך לראות אותה שוב.

הגדרה או החלפה של הסיסמה:

```bash
wrangler pages secret put PLATFORM_PASSWORD --project-name shul-board
```

מקומית: קובץ `.dev.vars` עם `PLATFORM_PASSWORD=...` (ב-gitignore).

### רשומת זמן תפילה

כל מניין ב-`rooms` הוא מחרוזת `"06:30"` (שעה קבועה, המבנה הישן) או אובייקט:

```js
{ type: 'fixed',    time: '06:30',                                  days: [0, 1, 2, 3, 4] }
{ type: 'relative', base: 'sunrise', offset: -45, round: 5, show: 'text', days: [] }
```

- `base` — כל מפתח מ-`ZMANIM_DEFS` (כולל דריסות של הגבאי), וגם `candle` (הדלקת נרות) ו-`havdalah`
  (צאת השבת) שמגיעים מהקשר השבת.
- `offset` — דקות חתומות, שלילי = לפני. `round` — עיגול השעה המחושבת (0 = בלי).
- `show` — `time` (ברירת מחדל) מציג את השעה המחושבת; `text` מציג את הטקסט הגולמי
  ("45 דק׳ לפני הנץ החמה"). גם במצב טקסט השעה מחושבת — למיון ול"התפילה הבאה".
- `days` — ריק = כל הימים; 0 = ראשון … 6 = שבת.

`minchaErevOffsets` ו-`arvitMotzashOffsets` היו פעם רשימות מספרים (דקות מהדלקת נרות /
מצאת השבת). היום הן רשימות רשומות כמו כל השאר; מספר ישן מומר ב-`normalize()` (בשרת,
בניהול ובצג) לרשומה יחסית לאותו בסיס. האשף עדיין עורך אותן כמספרים, ונוגע רק ברשומות
"פשוטות" — יחסיות לאותו בסיס, בלי עיגול ובלי תצוגת טקסט.

הפתרון בצג: `resolveTimeEntry()` ב-`display.js`. העורך: `timeEntryRow()` ב-`admin.js`.

### סגנון "פנינה"

הסגנון היחיד שיושב כולו ב-`css/board.css` בלי נכסים חיצוניים: מסגרת זהב כפולה
(`box-shadow: inset` בשתי שכבות), ארבע זוויות פינה עם "פנינה" קטנה, ושורות בקופסאות
בהירות עם פס בקצה הפותח.

- **בלי SVG ובלי אלמנטים נוספים ב-DOM**: כל שנים-עשר הסימנים של הזוויות מצוירים
  כשכבות רקע על `.card::before` אחד (שמונה `linear-gradient` לזרועות, ארבעה
  `radial-gradient` לפנינים). לכן הוא חד בכל גודל מסך ולא מתעוות כמו `border-image`.
- כל הצבעים נגזרים מ-`--accent` ומ-`--text` דרך `color-mix`, ולכן הסגנון עובד עם כל
  ערכות הצבע — בהירות וכהות. חמישה משתנים מקומיים (`--pearl-line`, `--pearl-band`,
  `--pearl-corner`, `--pearl-row`, `--pearl-chip`) מרוכזים בראש הבלוק.
- ערכת הצבע `ivory` ("שנהב") נבנתה יחד איתו: קרם עם זהב חם, ורקע של שלושה פסי אור
  אלכסוניים חלשים + גרעיניות `feTurbulence` + וינייטה. ב-data URI חובה לקודד `#` כ-`%23`.
- המראה `pearl` ותבנית `ornate` ("לוח מוזהב") הם נקודת הפתיחה המוכנה.

הסגנון הקודם `elements` (מסגרות SVG מגולפות) וקוביית `decor` (אלמנטים גרפיים) הוסרו
במיגרציה 5. `css/elements.css`, `js/decor.js` ו-`tools/gen-decor.py` נמחקו.
- **כותרת מותאמת**: `title` על כל קובייה שמציגה כותרת. בקוביות שמצוירות מ-`display.js`
  היא עוברת ל-`mkCard()` דרך `cardTitle()`; בקוביות שמעבירות אלמנט קיים (זמנים, תפילות,
  הנצחות) היא נכתבת ל-`h2` והמקור נשמר ב-`_homes` ומוחזר ב-`restoreClassic()`. הכותרת
  הדינמית של התפילות ("זמני תפילות שבת") מכבדת את הדריסה דרך `_blockTitles`.
- **תזמון מסך**: על המסך עצמו — `days` (0–6, ריק/שבעה = תמיד), `from`/`to` (תאריך לועזי)
  ו-`fromTime`/`toTime` (`HH:MM`, טווח שחוצה חצות נתמך). `screenActive()` ב-`display.js`
  מסנן, ו-`screensConfig()` מחזיר רק את המסכים הפעילים; אם אין כאלה — המסכים בלי תזמון.
  היום נמדד לפי `getEffectiveHDate().getDay()`, כך שאחרי השקיעה זה כבר היום הבא.
  טיימר דקתי בודק אם קבוצת המסכים הפעילים השתנתה. בתצוגה מקדימה הסינון מושבת.
- קוביית `header`: `showClock: false` ו-`showSub: false` מוסיפים למחלקות של `.display-header`
  את `no-clock` / `no-sub` (ב-`screens.css`), כך שנשאר רק שם בית הכנסת. `restoreClassic()`
  מסיר את המחלקות בחזרה לפריסה הקלאסית.
- קוביית `media`: `fit` (`contain`/`cover`, ברירת מחדל = ההגדרה של חלון המודעות) ו-`page`
  (`portrait`/`landscape`/`square`/`fill`). PDF לא מכבד `object-fit`, ולכן `renderMediaItem()`
  מחשב ל-iframe מידות בפיקסלים לפי יחס הדף וגודל הקובייה — כך כל הדף נכנס ללוח.
  יכולות להיות כמה קוביות מדיה במסך אחד, כל אחת עם ההתאמה שלה.
- עורך הפריסה: הקוביות מעוגנות בשמאל (`style.left`, `x` נמדד משמאל), וידית שינוי הגודל
  יושבת בפינה הימנית-תחתונה. לכן גרירה ימינה ולמטה מגדילה.

### "אין תחנון" ותאריכים עבריים

הרשימה שקובעת מתי מופיע "אין תחנון" ניתנת לעריכה מהניהול (לשונית "חגים ואירועים"):

- **הכללים הקבועים** ב-`TACHANUN_RULES` (`presets.js`) — 14 כללים (ראש חודש, ניסן, פסח שני,
  ל״ג בעומר, סיוון עד י״ב, ט׳ באב, ט״ו באב, ערב ראש השנה, תשרי מט׳ ואילך, חנוכה, ט״ו בשבט,
  פורים ושושן פורים, פורים קטן, שבת). המימוש שלהם ב-`TACHANUN_TESTS` ב-`display.js`, לפי מזהה.
- **ההגדרה** ב-`config.tachanun`: `rules` (מפה של מזהה → בוליאני) ו-`extra` (רשימת ימים
  נוספים של הקהילה, כל אחד `{name, hebrewDay, hebrewMonth}`). הניהול כותב ל-`rules` **רק
  ערכי `false`**, כך שכלל חדש שיתווסף בעתיד יידלק לבד לכל מי שכבר שמר. הצג קורא
  `ruleOn(id)` = `rules[id] !== false`.
- כלל חדש: מוסיפים רשומה ל-`TACHANUN_RULES` ופונקציה ב-`TACHANUN_TESTS` באותו מזהה. אין
  צורך במיגרציה.

**התאמת חודש עברי** (`hebMonthMatches()` ב-`display.js`): הניהול שומר שם חודש בלי ניקוד
("אלול"), ו-`@hebcal/core` מחזיר שם מנוקד ("אֱלוּל"). ההשוואה מתבצעת אחרי הסרת ניקוד,
גרשיים ורווחים כפולים, ומקבלת גם מספר חודש. אותה פונקציה משרתת את היארצייט
(`hebrewDateMatches()`), את `eventAppliesToday()` ואת הימים הנוספים של "אין תחנון".

**יארצייט בלבד**: לרשומת הנצחה יש `onlyOnDay` — כשהוא דלוק הרשומה מוצגת רק ביום היארצייט
עצמו. אם אף רשומה לא נשארת, קוביית ההנצחות מוסתרת כולה. הדגל נכלל גם בייבוא/ייצוא ה-CSV
(עמודה "רק ביום").

### ייצוא לאופליין

`public/js/export-offline.js` (נטען רק בניהול) בונה קובץ HTML אחד שרץ מ-`file://` בלי רשת.
הבנייה בדפדפן של הגבאי ולא ב-Worker: המדיה יכולה להגיע לעשרות מגה־בייט.

מה נארז: `display.html` עם שלושת גיליונות הסגנון וקוד הצג מוטמעים, חבילות
`@hebcal/core` ו-`@hebcal/learning` (השנייה נטענת בצג בעצלנות — כאן היא חייבת להיות
מראש), הגופנים מ-Google Fonts כ-data URI, המנה הציבורית של בית הכנסת, וכל קובץ מדיה.

- **גופנים**: ה-CSS של Google מפוצל לתת-קבוצות לפי טווח תווים עם הערה לפני כל בלוק
  (`/* hebrew */`). נשמרות עברית ולטינית בלבד. מתווסף `Orbitron` שמראה השעון "לד"
  טוען בעצלנות.
- **מדיה**: כל מחרוזת בצורת `/m/<id>` במנה נאספת ונשמרת כ-data URI. בצג
  (`offlineBundle()` ב-`display.js`) כל אחת הופכת ל-**blob URL** וכל הופעה שלה במנה
  מוחלפת — דפדפנים לא מרנדרים PDF מ-`data:` בתוך iframe, אבל כן מ-`blob:`.
- **`String.replace` עם מחרוזת החלפה אסור כאן**: `$&`, ``$` `` ו-`$'` הם תווי בקרה,
  וקוד CSS/JS מכיל אותם — זה שכפל חלקים מהדף לתוך עצמו. כל ההחלפות מקבלות פונקציה.
- **הטמעת JS ב-`<script>`**: רק `</script` מסוכן, ומוחלף ב-`<\/script` (חוקי בכל
  הקשר ב-JS). ב-JSON מספיק לברוח מ-`<` ל-`\u003c`.

בצד הצג, `window.SHUL_OFFLINE` מדליק מצב אופליין ב-`display.js`: `fetchBundle()` מחזיר
את המנה המוטמעת, `sendHeartbeat()` ו-`checkForUpdates()` לא פונים לשרת, ו-`ensureLearning()`
ו-`ensureFont()` לא מנסים למשוך מ-CDN. מזג האוויר עדיין מנסה — ונכשל בשקט, כמו בכל
נפילת רשת. הקובץ הוא תצלום של רגע הייצוא, אבל כל מה שמחושב (זמנים, תאריך עברי, פרשה,
יארצייט, תזמון מסכים) ממשיך להיות נכון כל יום.

## אנגלית

`public/js/i18n.js`. **עברית היא שפת המקור**: הטקסט העברי כתוב ישירות ב-HTML וב-JS,
והאנגלית היא שכבת כיסוי לפי מפתח. המשמעות המעשית — מחרוזת חדשה שנוספת בלי תרגום
פשוט נשארת בעברית, ואף פעם לא מופיע מפתח חשוף על הצג.

- ב-HTML: `data-i18n="key"` (וגם `data-i18n-ph` / `-title` / `-aria`).
  `I18n.apply()` מחליף רק מפתחות שיש להם תרגום.
- ב-JS: `T('key', 'העברית שכבר כתובה כאן')` — עוזר מקומי ב-`display.js` שנופל
  לאחור לארגומנט השני.
- `setLang` מחליף גם `dir`/`lang` על `<html>` וגם את הלוקאל של hebcal, כך שפרשה,
  חגים ותאריך עברי חוזרים באנגלית (`26th of Elul, 5786` במקום `כ״ו אלול תשפ״ו`).

**שפת הצג היא הגדרה של בית הכנסת** (`config.display.lang`) ולא של הדפדפן — על מסך
בקיר אין מי שילחץ על בורר. היא נבחרת בניהול ב"מראה הצג" → "מה מוצג על הלוח",
והתצוגה המקדימה מתחלפת מיד.

**מה לא מתורגם, בכוונה**: כל מה שהגבאי הקליד — שם בית הכנסת, שמות חדרים ומניינים,
הנצחות, הודעות, הקדשות ושמות אירועים. זה התוכן שלו, בשפה שבחר.

**כיווניות**: ה-CSS כמעט כולו לוגי (`inline-start/end`, `text-align: start`), ולכן
`dir` על `<html>` מספיק. המקומות שבהם הכיוון באמת חשוב מרוכזים בבלוק "כיווניות"
ב-`board.css`: תאי השעה (שמחזיקים `direction: ltr` לספרות ולכן `start/end` בתוכם
לא נקבעים לפי כיוון הלוח), ופס ההודעות — שרץ תמיד לכיוון הקריאה, כלומר משמאל
לימין בעברית ומימין לשמאל באנגלית.

## יציבות לבתי כנסת פעילים

צג שכבר רץ בבית כנסת חייב להמשיך לעבוד גם כשמשנים קוד או מבנה נתונים.
הכל ב-`functions/_migrations.js`, בצד השרת, כך שגם צג עם קוד ישן מקבל נתונים שהוא מבין:

| מנגנון | מה הוא עושה | מתי לגעת |
|---|---|---|
| `SCHEMA_VERSION` + מיגרציות | `config.schemaVersion` אומר באיזה מבנה נשמרו הנתונים. בכל קריאה הם מועלים **בזיכרון** לגרסה הנוכחית; למסד נכתב המבנה החדש רק כשהגבאי שומר שוב | כששדה משנה משמעות או מבנה: מעלים את המספר ומוסיפים פונקציה ל-`MIGRATIONS` |
| `normalize()` | כל מקטע מושלם בברירות מחדל ומנוקה: קובייה עם סוג לא מוכר נזרקת, קואורדינטות נחתכות לרשת, מערכים חסרים מתמלאים. רץ גם לפני כתיבה | כשמוסיפים שדה: מספיק להוסיף אותו ל-`DEFAULTS` |
| `APP_VERSION` | מצורף למנה הציבורית ולנכסים (`?v=`). הצג טוען את עצמו מחדש כשהגרסה משתנה, כך שקוד ישן לעולם לא רץ מול נתונים חדשים | בכל שינוי ב-`display.js` או ב-css של הצג |

תכונות חדשות דולקות רק להרשמות חדשות: מיגרציה 4 מכבה את `todayLine`, `fastTimes`,
`nextHighlight` ו-`extendedMentions` לכל מי שנשמר לפניהן; הזמנים החדשים כבויים; המקטעים
החדשים ריקים; והקוביות החדשות מופיעות רק אם מוסיפים אותן.

כשמסירים תכונה נוהגים באותה זהירות: מיגרציה 5 מעבירה כל מי שהיה על הסגנון `elements`
לסגנון `pearl` ואת המראה `palace` למראה `pearl`, וקוביות `decor` נזרקות בשקט ב-`normalizeBlock`
כי הסוג ירד מ-`KNOWN_BLOCKS`. הצג לא נשבר, ובשמירה הבאה הנתונים נכתבים נקיים.

בנוסף, כל קובייה בצג מצוירת בנפרד (`safe()` ב-`display.js`): תקלה בהנצחות לא משאירה
את הזמנים ריקים, וכישלון בטעינת נתונים משאיר את מה שכבר מוצג.

## קטלוג העיצוב

`public/js/presets.js` הוא קטלוג אחד שמשותף לניהול ולצג:

| | מה זה | כמה |
|---|---|---|
| מראות (`DESIGN_PRESETS`) | כרטיס אחד = ערכת צבע + סגנון + פריסה + גופן | 21 |
| ערכות צבע (`THEMES`) | `data-theme` על `body` — הצבעים ב-`css/board.css` | 20 |
| סגנונות (`STYLES`) | `data-style` — מסורתי מפואר, קלאסי, מודרני, מינימליסטי, מעוטר, זכוכית, נועז, פנינה | 8 |
| פריסות (`LAYOUTS`) | `data-layout` של הצג הקלאסי | 6 |
| גופנים (`FONTS`) | `data-font` — קלאסי, ספרותי, דוד, נקי, היבו, רוביק, סקולר, אלף, ורלה | 9 |
| תבניות מסך (`SCREEN_TEMPLATES`) | סידור קוביות מוכן על רשת 24×18: לוח מוזהב, לוח מחוונים, לוח שבת, לוח קהילה, לוח לימוד, אנכי ועוד | 14 |
| קוביות (`BLOCKS`) | סוגי הקוביות שהצג יודע לצייר | |

שדות נוספים על קובייה נשמרים כמו שהם (`normalizeBlock` שומר את כל המפתחות). קוביית השעון:
`variant` (`digital`/`analog`), `face` (דיגיטלי: `plain`/`thin`/`seven`/`flip`; אנלוגי:
`classic`/`numbers`/`roman`/`hebrew`/`minimal`/`modern`), `seconds` (ברירת מחדל true),
`showDate` (דיגיטלי בלבד). הציור ב-`analogClockSvg()` וב-`clock()` ב-`display.js`; הרשימה
לעורך ב-`CLOCK_FACES` ב-`admin.js`. הגופן של מראה "לד" (Orbitron) נטען בעצלנות רק כשיש קובייה כזאת.

בנוסף לכוונון עדין: צבע הדגשה חופשי (`--accent`), גודל טקסט (`--fs`, 70%–150%),
תמונת רקע (העלאה ישירה או מהקבצים שהועלו) עם התאמה (`backgroundFit`: cover/contain/stretch/tile),
החשכה (`backgroundOverlay`) וטשטוש (`backgroundBlur`), ולוגו. `config.design` שומר את כל השדות;
`resolveDesign()` משלים ברירות מחדל כך שהגדרות ישנות ממשיכות לעבוד.
בחירת מראה כותבת את השדות; כוונון ידני מאפס את `preset` והכרטיס מסומן שוב רק
כשהערכים חוזרים להתאים.

מקורות נתונים בצג: זמנים ולוח שנה מ-`@hebcal/core` (כולל `Molad`), לימוד יומי מ-`@hebcal/learning`
שנטען בעצלנות רק כשיש קובייה, מזג אוויר מ-Open-Meteo (בלי מפתח, כל 30 דק׳),
איתור כתובת מ-Nominatim.

## מסכים בלייב

כל צג שולח לשרת דופק כל 3 דקות (`POST /api/heartbeat/:slug`) עם מזהה יציב, גודל
המסך, גרסת הקוד וחותמת הנתונים שהוא מציג. מסך שדיווח ב-7 הדקות האחרונות נחשב
"בלייב"; מסך שלא נראה שבוע נעלם מהרשימה.

- הדופק **מחליף** את בדיקת העדכונים הישנה — התשובה כוללת את חותמת הגרסה הנוכחית,
  כך שמספר הבקשות למסך לא גדל.
- בלי `?screen=שם`, הצג שומר מזהה אקראי בדפדפן; במצב "גלישה בסתר" המזהה מתחלף בכל
  הפעלה, ולכן טביעת-אצבע של המכשיר מאחדת מסך שהופעל מחדש עם קודמו.
- התצוגה המקדימה בניהול (iframe) לא נספרת.
- הטבלה `screens` נוצרת בעצלנות מהקוד.

| נתיב | מי | מה |
|---|---|---|
| `POST /api/heartbeat/:slug` | הצג (ציבורי) | דופק; מחזיר `{version, appVersion}` |
| `GET /api/screens` | גבאי | המסכים של בית הכנסת, מי בלייב ומה הוא מציג |

## תבניות מהקהילה

התבנית היא צילום של המצב **השמור**: `design` (בלי הלוגו), `screens` ו-`display`, בלי שום תוכן.
לחיצה מעתיקה מראה ופריסה בלי לגעת בלוגו ובתוכן. עד 5 תבניות לבית כנסת; מונה שימושים;
המפרסם יכול להסיר. הטבלה `templates` נוצרת בעצלנות מהקוד, כך שפריסה לא דורשת הרצת סכימה.

| נתיב | מי | מה |
|---|---|---|
| `GET /api/templates` | ציבורי, cache דקה | רשימת התבניות הציבוריות |
| `POST /api/templates` | גבאי | פרסום המראה השמור `{name, description}` |
| `POST /api/templates/:id` | גבאי | ספירת שימוש |
| `DELETE /api/templates/:id` | הגבאי שפרסם | הסרה |
| `DELETE` / `PATCH {status}` `/api/admin/template/:id` | מנהל המערכת | הסרה או הסתרה של תבנית לא ראויה |

## המתקין ל-Windows

לא מקמפלים קובץ לכל בית כנסת. יש **מתקין גנרי אחד**, וה-Function מגיש אותו תחת
שם קובץ אישי — `ShulBoard-Setup-<slug>.exe`. המתקין קורא את `$EXEFILE` של עצמו,
מחלץ את המזהה, ומתקין קיצור דרך + הפעלה אוטומטית של הדפדפן במצב קיוסק.

בניית הקובץ (חד-פעמית):

```bash
sudo apt-get install -y nsis
makensis -DBASE_URL=https://shul-board.pages.dev installer/shulboard.nsi
mkdir -p public/assets && mv installer/ShulBoard-Setup.exe public/assets/
wrangler pages deploy --project-name shul-board --branch main
```

הקובץ הבנוי לא נשמר בריפו (`.gitignore`). עד שהוא נבנה, `/download/ShulBoard-<slug>.bat`
נותן את אותה תוצאה בלי התקנה: פותח את הצג במסך מלא ומעתיק את עצמו לתיקיית ההפעלה
האוטומטית של Windows. ה-Function בודקת חתימת `MZ` לפני שהיא מגישה EXE, כדי לא לשלוח
HTML בטעות.

## פיתוח ופריסה

```bash
npm install
wrangler d1 execute shul-board --local --file=schema.sql   # פעם אחת
wrangler pages dev --port 8790
```

**הפריסה אוטומטית, ומגיעה מ-Cloudflare עצמה.** הפרויקט ב-Pages מחובר לריפו
(Settings → Builds & deployments → "Automatic deployments enabled"), ולכן:

| דחיפה ל… | מה נוצר |
|---|---|
| `main` | פריסת **Production** — `shul-board.pages.dev` |
| כל ענף אחר | פריסת **Preview** בכתובת אקראית, לבדיקה לפני מיזוג |

אין צורך ב-GitHub Action, ולא היה: היה כאן workflow שהריץ `wrangler pages deploy`
והוא נמחק, כי הוא פרס כל דחיפה פעם שנייה. `npm run deploy` עדיין עובד להקמה
ראשונית של עותק חדש, אבל לא צריך אותו בשוטף.

### המלכודת שעולה הכי הרבה זמן

**סוד שמוגדר ב-Pages לא נכנס לתוקף בפריסה שכבר רצה.** משתני הסביבה נקשרים
לפריסה ברגע שנוצרה; הוספת סוד בדשבורד לא נוגעת בפריסה החיה. אחרי כל שינוי סוד
צריך פריסה חדשה — דחיפה כלשהי, או Deployments → ⋯ → **Retry deployment**.

שתי מלכודות נוספות מאותה משפחה:

- ל-Pages יש **שתי קבוצות משתנים נפרדות**, Production ו-Preview. סוד שהוגדר
  ב-Preview בלבד אינו קיים בפרודקשן.
- שם עם שגיאת הקלדה נראה בדיוק כמו סוד חסר. (קרה: `wrangGAS_MAIL_URL` מהדבקה
  שגויה, ושבועיים של חיפוש באג בקוד שלא היה בו באג.)

בגלל שלושת אלה יש בדשבורד המנהל (`/manage`) פאנל **"תכונות אופציונליות"**:
הוא שואל את ה-Function עצמה אילו סודות היא באמת רואה, ומחזיר בוליאנים בלבד —
הערכים לעולם לא יוצאים משם. כשתכונה "לא עובדת", זו התחנה הראשונה.

המזהים של D1 ושל שני מרחבי ה-KV יושבים ב-`wrangler.toml`. בעותק משלכם מחליפים אותם
במזהים של המשאבים שיצרתם.

## המכסה החינמית

הכל רץ במסלול החינמי, בלי כרטיס אשראי. המגבלה הצרה ביותר היא **100,000 בקשות
ליום** ל-Functions:

- הצג מחשב שעון וזמנים **מקומית** ובודק שינויים כל 3 דקות → ~480 בקשות ליום למסך.
  הבדיקה הזאת היא גם הדופק של "מסכים בלייב" — כתיבה אחת ל-D1 בכל פעם, מתוך 100,000
  כתיבות ליום במסלול החינמי.
- המנה הציבורית נשמרת ב-cache של הקצה ל-60 שניות, כך שכמה מסכים באותו בית כנסת
  חולקים תשובה אחת.
- מדיה מוגשת עם `immutable` לשנה — נטענת מה-CDN, לא מה-Function.

מגבלות נוספות: D1 — 5GB; KV — 1GB אחסון, 1,000 כתיבות ליום. לכן מדיה מוגבלת
ל-5MB לקובץ ו-60MB לבית כנסת. R2 (10GB) היה מרווח יותר אבל דורש כרטיס אשראי
גם במסלול החינמי, ולכן לא בשימוש.
